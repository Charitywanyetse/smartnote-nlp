services:
  - type: web
    name: smartnote-nlp
    runtime: python
    buildCommand: chmod +x setup.sh && ./setup.sh
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9